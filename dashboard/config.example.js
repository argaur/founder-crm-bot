// Copy this file to config.js and fill in your values.
// config.js is gitignored — never commit it.

const AIRTABLE_PAT     = 'YOUR_AIRTABLE_PAT';       // Read-only PAT scoped to data.records:read
const AIRTABLE_BASE_ID = 'YOUR_AIRTABLE_BASE_ID';   // Starts with "app..."
const USER_ID          = 'YOUR_USER_ID';             // The user_id from /register
const REFRESH_INTERVAL_MS = 5 * 60 * 1000;          // Auto-refresh every 5 minutes
